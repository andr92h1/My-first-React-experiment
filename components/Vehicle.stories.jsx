import React, { useState, useEffect } from "react";

import { VehicleInfo } from "./Vehicle";

export const AndreeaLabel = () => {
  return <VehicleInfo />;
};

export default {
  title: "Vehicles",
};
